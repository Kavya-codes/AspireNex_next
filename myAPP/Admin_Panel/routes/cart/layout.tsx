export const metadata = {
  title: "Cart | Kemal Store",
  description: "Cart for e-commerce, selling products, and new productivity",
};

const Layout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default Layout;
